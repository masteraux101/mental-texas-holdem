name: publish

on:
  workflow_dispatch:
  push:
    branches:
      - master

permissions:
  contents: read

concurrency:
  group: "build"
  cancel-in-progress: true

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install dependencies
        run: npm ci
      - name: Build
        run: npm run build
        env:
          PUBLIC_URL: /mental-texas-holdem
      - name: Rename index.html to main.html
        run: |
          mv build/index.html build/main.html
      - name: Prepare artifact
        run: |
          mkdir -p artifact/mental-texas-holdem
          cp -r build/* artifact/mental-texas-holdem/
      - name: Push to github pages repo
        uses: cpina/github-action-push-to-another-repository@main
        env:
          API_TOKEN_GITHUB: ${{ secrets.GITHUB_TOKEN }}
        with:
          source-directory: 'artifact'
          destination-github-username: 'masteraux101'
          destination-repository-name: 'masteraux101.github.io'
          user-email: github-action@github.com
          user-name: github-action
          target-branch: main
